conda create -n d2l-zh -y python=3.8 pip
                                    ::-n means env_name, -y means yes, pip create environment

conda activate d2l-zh              ::import

python                              ::use python grammar in the following
import torch                        ::import

                                    ::if error, try: import os
                                    ::os.environ['KMP_DUPLICATE_LIB_OK'] = 'TRUE'
                                    ::import torch

torch.version.cuda                  ::cuda version search
torch.cuda.is_available()           ::test cuda

exit()                              ::exit python environment

conda env list                      ::list env

